# Strategy Sandbox 宠物险肠胃炎理赔策略仿真 MVP

## 1. 产品定位

Strategy Sandbox 是“通用策略仿真内核＋行业解决方案包”平台。本 MVP 聚焦一个可以被确定性验证的问题：

> 上海地区宠物险肠胃炎案件，将 \`covered_expense\` 阈值从 200 元调整为 500 元，会产生什么业务影响？

平台不替代保险核心理赔系统，而是在策略影响客户前，通过冻结事实、并行回放、指标标记和案例证据链，回答“改了什么、影响谁、增加多少赔付、需要多少人工”。

首期用户包括理赔运营、理赔审核员、产品经理、精算与财务、风控、兽医专家、合规审计及解决方案架构师。

## 2. MVP 目标与非目标

### MVP 目标

- 使用同一份 FactSnapshot 回放现行和候选规则；默认实验只改变保障费用阈值，用户也可以编译当前事实白名单内的多条件候选规则。
- 演示病历、发票、检验报告、诊疗图片和用户病述的多模态证据链。
- 将OCR、诊疗图片人工审核、结构化事实和离线平台记录归一到跨平台本体图谱。
- 通过数据接入弹窗演示数据库或平台接口配置、连接测试、数据发现与动态气泡映射。
- 展示上海肠胃炎影响客群、覆盖建议迁移、赔付金额及人工工作量变化。
- 支持从汇总指标下钻到单案事实、证据、金额公式和关联证据路径。

### 非目标

- 不真实连接保单、医院、发票、支付、数据库或生产理赔系统；本轮数据接入仅执行本地确定性模拟。
- 不实现真实 OCR、医学影像诊断、欺诈预测或未来赔付预测。
- 不覆盖投保核保、续保、保全和完整反欺诈调查。
- 不允许 OCR、VLM、本体关系、家系关系或异常分直接形成最终拒赔。
- 不实现生产策略审批、授权执行和正式赔付。

## 3. 核心实验

### 3.1 策略定义

| 项目 | 现行策略 | 候选策略 |
|---|---|---|
| 策略版本 | \`PET-GI-BASELINE-1.0\` | \`PET-GI-CANDIDATE-1.1\` |
| 地区 | 上海 | 上海 |
| 疾病 | \`GASTROENTERITIS\` | \`GASTROENTERITIS\` |
| 比较符 | 严格大于 \`>\` | 严格大于 \`>\` |
| covered_expense 阈值 | 200 元 | 500 元 |
| 命中动作 | \`NOT_COVERED_RECOMMENDATION\` | \`NOT_COVERED_RECOMMENDATION\` |

影响客群：

\`\`\`text
region = SHANGHAI
AND disease = GASTROENTERITIS
AND 200 < covered_expense <= 500
\`\`\`

200→500仍是默认面试演示实验。策略实验页同时支持业务用户用自然语言定义当前FactSnapshot字段白名单内的候选规则；规则确认后，结构化条件、动作、DSL、规则检查和仿真使用同一份CandidateRule。

### 3.2 边界语义

严格大于号必须通过以下固定夹具验收：

| covered_expense | 现行 200 | 候选 500 |
|---:|---|---|
| 199 | 覆盖建议 | 覆盖建议 |
| 200 | 覆盖建议 | 覆盖建议 |
| 200.01 | 不覆盖建议 | 覆盖建议 |
| 499.99 | 不覆盖建议 | 覆盖建议 |
| 500 | 不覆盖建议 | 覆盖建议 |
| 500.01 | 不覆盖建议 | 不覆盖建议 |

### 3.3 金额口径

\`covered_expense\` 是免赔额和赔付比例计算之前，经保障范围核验后可覆盖费用明细的合计：

\`\`\`text
covered_expense = sum(covered eligible line items)

payable_amount = min(
  remaining_limit,
  max(0, covered_expense - deductible) × reimbursement_rate
)
\`\`\`

“不覆盖建议”是策略仿真建议，不等于生产系统的实际拒赔。
### 3.4 运营版自然语言策略编译

策略实验面向非技术运营人员，默认流程收敛为：

1. 用日常业务语言输入规则。
2. 点击“模型解析”，由本地 Node 代理调用 OpenAI 兼容 Chat Completions 接口。
3. 在简化中文表单中核对地区、疾病、判断条件、满足/不满足时的结果和数据缺失处理。
4. 点击唯一主操作“确认规则并进入检查”。

未点击“模型解析”前不生成解析结果。单条件不展示逻辑关系；多条件只显示“需要全部满足 / 满足任意一个”。运算符以“超过（不含）”“不少于（包含）”等中文表达展示，页面不向运营人员暴露字段代码、DSL、规则Hash、THEN/ELSE或候选版本差异。

技术与审计信息保留在默认关闭的“查看技术详情”中，包括现行/候选差异、边界样本、DSL、模型请求元数据和FactSnapshot。技术详情与中文表单读取同一份规则草稿，不存在第二套编辑入口。

模型输出必须通过服务端结构校验和现有确定性规则校验。字段白名单仍为 `covered_expense`、`deductible`、`reimbursement_rate`、`remaining_limit`、`material_complete`、`image_compliance`；只支持一层 `AND / OR`。材料完整度与诊疗图片人工审核只能进入人工复核、请求补件或沿用现行路由，不能产生自动审核、不覆盖建议或最终拒赔。模型调用失败时保留原输入，并显示可行动的中文错误；“暂用本地解析”必须由用户主动点击，生成规则标记为 `LOCAL_FALLBACK`。

### 3.5 模型设置与API代理

共享设置页路由为 `#/settings`，可配置OpenAI兼容Base URL、模型名称、API Key和5–120秒请求超时。公网Base URL只允许HTTPS，`localhost`与`127.0.0.1`允许HTTP；地址不得携带用户名或密码。

无外部依赖的 `server.js` 在4173端口同时提供静态页面和以下接口：

| 接口 | 用途 |
| --- | --- |
| `GET /api/model/settings` | 返回脱敏配置与连接状态 |
| `PUT /api/model/settings` | 更新进程内模型配置 |
| `DELETE /api/model/settings` | 清除进程内配置并回退环境变量 |
| `POST /api/model/test` | 使用当前表单配置测试连接，不提前保存 |
| `POST /api/rules/parse` | 调用模型并返回经过白名单校验的CandidateRule |

API Key优先读取 `MODEL_API_KEY` 环境变量。页面输入的密钥只在提交时到达本地代理，保存后仅存在服务进程内存；不会写入LocalStorage、接口响应、日志或配置文件，服务重启后需要重新提供。候选规则记录 `parseSource`、模型名称、请求ID和解析时间，用于审计，不记录提示词中的密钥或认证头。

## 4. 端到端架构

\`\`\`mermaid
flowchart LR
    material["本地材料：病历、票据、检验、图片、病述"] --> register["登记、哈希、材料分类"]
    register --> ocr["OCR：文本、表格、bbox、置信度"]
    register --> image["诊疗/手术图登记与人工审核"]
    image --> imageResult["ImageReviewResult"]
    structured["结构化案件数据"] --> normalize["字段校验与标准化"]
    ocr --> match["客户/保单/宠物/理赔 ID 匹配"]
    imageResult --> match
    normalize --> match
    platforms["离线平台记录：保险、医院、支付、宠物档案"] --> identity["跨平台身份匹配"]
    match --> identity
    identity --> ontology["本体实体与关系构建"]
    ontology --> evidencePath["关联证据路径"]
    evidencePath --> confirm["人工确认 / 补件 / 复核"]
    confirm --> snapshot["冻结 FactSnapshot"]
    snapshot --> compiler["自然语言策略编译与歧义检查"]
    compiler --> rules["结构化CandidateRule与DSL"]
    rules --> baseline["现行规则 200 元"]
    rules --> candidate["候选规则 500 元"]
    baseline --> compare["指标比较与案件迁移"]
    candidate --> compare
    compare --> drill["案例、证据、金额与关联证据路径下钻"]
\`\`\`

流程不使用 SOP 步骤条。各能力通过 Hash 路由进行整页切换，浏览器前进、后退和刷新均可恢复页面。

## 5. 信息架构

| 页面 | 路由 | 核心能力 |
|---|---|---|
| 材料审核 | \`#/pet/intake\` | 文本材料OCR核对；诊疗图片人工结论、问题标签、条件必填意见与审核留痕 |
| 本体图谱 | #/pet/graph | 数据库/平台数据接入；宠物、平台、数据类别和具体记录的跨平台关联；家系与风险线索 |
| 数据与快照 | \`#/pet/datasets\` | 人工确认后的事实冻结、版本、哈希、固定种子和边界夹具 |
| 策略实验 | \`#/pet/strategies\` | 运营自然语言输入、模型解析、中文表单修正、单一确认操作；技术详情默认折叠 |
| 规则检查 | \`#/pet/validation\` | 结构、字段、逻辑、比较符、空值、THEN/ELSE、动作权限和快照绑定检查 |
| 仿真总览 | \`#/pet/simulation\` | 同一快照执行通用候选规则；按规则类型展示费用分布或条件命中、建议/路由构成和案件迁移 |
| 案例追踪 | \`#/pet/cases\` | 迁移筛选、新旧建议与路由、逐条件真假、空值策略、THEN/ELSE分支、费用与证据 |

信贷解决方案继续使用 \`#/credit/...\`，旧 \`#/datasets\` 等地址保持兼容。

宠物险静态资源版本统一升级为 v16；宠物险与信贷路由保持不变。

宠物险MVP只允许使用“上海宠物险肠胃炎理赔 · 2026 Q2”数据集，避免数据集、FactSnapshot和仿真样本口径不一致。案例追踪将迁移范围集中展示一次，列表按15笔分页，仅保留可覆盖费用、赔付变化、处理路由和证据状态等具有区分度的字段。

## 6. 核心对象

### 6.1 PetClaim

包含案件、保单、宠物、投保人、地区、医院、疾病、费用明细、\`covered_expense\`、免赔额、赔付比例、剩余额度、材料完整度和历史结果。

### 6.2 EvidenceItem

包含材料类型、文件元数据、会话内预览、来源事实和人工确认状态。病历、发票、检验报告等文本材料保存OCR结果、bbox和置信度；诊疗图片不生成OCR字段，改为关联人工ImageReviewResult。用户文件内容只保留在当前浏览器会话；刷新后只保留非敏感元数据。

### 6.3 ImageReviewResult

最小字段：

- \`review_id\`、\`claim_id\`、\`artifact_id\`、\`material_type\`
- \`image_compliance\`：\`PASS / FAIL / NEEDS_REVIEW\`
- \`tag_code\`、\`tag_label\`：必选的问题标签枚举
- \`review_comment\`：PASS时选填，FAIL或NEEDS_REVIEW时必填
- \`reviewer_id\`、\`reviewed_at\`
- \`review_version\`、\`source_hash\`、\`status\`

问题标签按结论限制：PASS仅允许“材料一致”；FAIL允许图像模糊、主体不一致、诊疗部位不匹配、日期异常、疑似重复和其他异常；NEEDS_REVIEW在上述异常标签外增加“信息不足”。人工审核结果只能改变处理路由，不能直接拒赔。

### 6.4 FactSnapshot

包含冻结后的 CanonicalFact、数据版本、OCR/人工图片审核/本体图谱版本、平台记录及关系版本、内容哈希、固定种子和冻结时间。快照中的本体图谱版本字段统一命名为 \`ontology\`。本 MVP 固定种子为 \`20260814\`，默认人工审核版本为 \`HUMAN-IMAGE-REVIEW-1.0\`，默认本体图谱版本为 \`PET-ONTOLOGY-1.0.0\`，默认快照为 \`FS-PET-GI-SH-2026Q2-01\`。快照内容哈希必须覆盖平台记录及关系版本，并加入外接数据源稳定指纹、数据版本和记录摘要。任一本体关系、数据连接或版本发生变化后，已有 FactSnapshot、候选策略确认、规则检查和仿真结果均失效。

### 6.5 跨平台本体图谱

图谱采用焦点气泡视图，当前实体固定在画面中心，上级与直接下级作为附属气泡环绕。默认中心为宠物主实体；点击平台、类别或记录气泡后，被点击气泡平滑移动并放大到中心，原中心退回上级位置，附属气泡按当前实体的一度关系重新展开。叶子记录使用记录时间、关系状态、来源证据和置信度属性气泡补充上下文。

图谱上方显示从宠物主实体到当前中心的路径导航，并提供“返回上级”和“回到团子”。焦点切换只改变会话内视图状态，不写入URL、不改变本体关系、FactSnapshot或仿真缓存；页面刷新后回到宠物主实体。

聚焦动画使用原生SVG坐标插值，切换窗口为2秒，缓动为 cubic-bezier(.22,.8,.3,1)。外层节点始终锁定最终布局坐标，动画层只负责平移和缩放，因此中心节点最终精确落在视图中心。切换仅局部更新图谱、关联证据与右侧详情，不重建侧栏、顶栏或主页面，也不改变滚动位置。中心气泡保持完整清晰，其他关联气泡以低透明度和低对比度虚化；系统检测 prefers-reduced-motion，需要减少动态效果时直接更新视图。

默认演示宠物为“团子 · ****668”，展示品种和脱敏主编号。平台局部编号、主人联系方式、账户及芯片号必须脱敏，例如 \`HIS-****128\`，不得在界面或持久化状态中保留完整敏感标识。

跨平台身份匹配按以下优先级执行：

1. 芯片号或平台宠物编号精确匹配。
2. 保单登记宠物与医院档案匹配。
3. 宠物名称、品种、出生日期及主人脱敏联系方式组合匹配。

每条本体关系保存起止实体、关系类型、来源平台、匹配方式、置信度、证据、生成时间、有效期和确认状态。确认状态统一为 \`EXPLICIT\`、\`MATCHED\`、\`HUMAN_CONFIRMED\`、\`NEEDS_VERIFICATION\`。医院、支付和家系信息只能形成事实或风险线索，不得直接产生拒赔结论。

工作流使用 \`ontologyBuilt\` 和 \`ontologyConfirmed\`。首次读取旧版本状态时自动映射原图谱构建与确认进度，避免已有演示进度丢失；只有 \`ontologyConfirmed\` 为真时才允许冻结 FactSnapshot。

### 6.6 离线平台数据

MVP 内置固定离线演示数据，不调用外部接口。以下平台名称均为虚构演示名称，界面不额外添加“模拟”标签：

| 平台 | 平台类型 | 数据类别 |
|---|---|---|
| 大西洋保险 | 保险 | 投保信息、保单、当前理赔、历史理赔 |
| 浙江华西医院 | 医疗 | 就诊记录、诊断、检验、处方、费用票据、诊疗图片 |
| 协合医院 | 医疗 | 历史就诊、历史诊断、检查与用药 |
| 支付吧平台 | 支付 | 支付订单、收款账户、退款、疑似重复支付 |
| 萌宠档案平台 | 宠物档案 | 宠物身份、芯片、主人、同窝与家系关系 |

平台气泡显示平台类型与记录数量，类别气泡显示类别名称，记录气泡显示脱敏局部编号。根节点最多展示10个平台，其中外接平台最多5个；达到上限时禁止新增。中心气泡直径约132px，关联气泡约74–92px，属性气泡约54–64px。顶部保留平台类型图例和“人工确认本体关系”；右侧面板随中心实体显示平台、类别、局部编号、记录时间、匹配依据、置信度、有效期和关联状态。

### 6.7 本体图谱接口

| 接口 | 最小语义 |
|---|---|
| \`OntologyEntity\` | 实体ID、本体类型、名称、脱敏编号、属性及来源 |
| \`PlatformSource\` | 平台ID、平台名称、平台类型、数据版本及同步时间 |
| \`PlatformRecord\` | 平台局部ID、所属类别、事件时间、摘要和敏感级别 |
| \`OntologyRelation\` | 起止实体、关系类型、匹配方式、来源、置信度、证据、生成时间、有效期和确认状态 |
| \`AssociationEvidencePath\` | 宠物主实体、平台局部实体、类别记录、关系集合、身份匹配依据、来源证据及确认状态 |
| \`OntologyFocusView\` | 当前中心、上级、直接子节点、属性气泡、路径导航和当前关系集合 |
| \`OntologyBubbleNode\` | 实体、气泡角色、半径、角度、目标坐标及是否可交互 |

关联证据路径必须能从当前节点回溯到宠物主实体，且展示平台局部实体编号、身份匹配依据、数据类别、具体记录、来源证据和确认状态。

### 6.8 DataConnection 与本地模拟接入

图谱标题卡右上角提供“数据接入”。单一弹窗支持两类配置：

- 数据库：MySQL、PostgreSQL、SQL Server；配置接入名称、业务类型、主机、端口、数据库/Schema、用户名和密码。
- 其他平台：REST API、SFTP、Webhook；配置平台名称、业务类型、服务地址、认证方式、App ID和Token。
- 业务类型统一为保险、医疗、支付、宠物档案、其他，用于确定气泡颜色和确定性数据模板。

本轮“测试连接”和数据发现均在浏览器本地模拟，不访问外部网络。测试通过后展示数据类别、记录量、数据版本、同步时间和身份匹配依据；确认接入后生成独立平台气泡，关系状态默认为 NEEDS_VERIFICATION。断开连接会移除该平台、类别与记录。

DataConnection 仅持久化数据源ID、接入类型、名称、业务类型、协议或数据库类型、脱敏地址、连接状态、身份匹配字段、类别与记录摘要、数据版本、同步时间和稳定指纹。密码、Token与完整敏感地址只存在于当前弹窗草稿，关闭后立即清除，禁止写入 localStorage。

接入或断开数据源保留材料审核、图像人工审核和已构建图谱，但使 ontologyConfirmed、FactSnapshot、候选策略确认、规则检查及仿真结果失效。真实接入需通过后端连接网关、密钥管理、最小权限账号和平台适配器实现，浏览器不得直接连接生产数据库。
### 6.9 CandidateRule 与 RuleCondition

\`CandidateRule\` 保存策略ID、版本、原始自然语言意图、人工修正后的规则摘要、解析器版本、适用范围、单层 \`AND / OR\`、条件集合、THEN动作、ELSE动作、空值策略、FactSnapshot哈希和确认时间；可选审计字段包含 \`parseSource: MODEL | LOCAL_FALLBACK\`、\`modelName\`、\`modelRequestId\` 与 \`modelParsedAt\`。\`RuleCondition\` 保存字段、运算符、值、类型和单位。

确认后的规则写入 \`strategy-sandbox:pet-gi:v1:candidate-rule\`。旧阈值状态首次加载时自动迁移为上海肠胃炎、\`covered_expense > threshold\`、THEN不覆盖建议、ELSE覆盖建议、路由沿用现行的CandidateRule。未确认的输入草稿不写入持久化状态，也不能影响规则检查或仿真。

\`RuleParseIssue\` 使用 \`BLOCKING / WARNING\` 分级，至少保存问题代码、目标字段、信息和修正建议。修改结构化草稿时不影响已保存候选规则；重新确认并保存后保留FactSnapshot，同时使旧规则检查和仿真结果失效。

### 6.10 ClaimDecision

保障建议和处理路由分离：

- 保障建议：\`COVERED_RECOMMENDATION\`、\`NOT_COVERED_RECOMMENDATION\`
- 处理路由：\`AUTO_REVIEW\`、\`MANUAL_REVIEW\`、\`REQUEST_MORE\`

同时保存可赔金额、适用范围、命中规则、风险线索、解释和证据引用。

## 7. 数据与边界案例

使用固定种子生成不少于 300 笔案件，并提供20–50笔完整演示夹具。覆盖：

- 上海与非上海、肠胃炎与其他疾病。
- 199、200、200.01、499.99、500、500.01 元阈值边界。
- 同义词、否定表达、低置信度疾病归一。
- 缺失材料、OCR小数/日期/单位/合计错误、多费用明细。
- 诊疗图片人工审核PASS、FAIL、NEEDS_REVIEW及其问题标签、条件必填意见。
- 重复材料、共享账户、家系疾病聚集和身份匹配歧义。

家系和关联风险只是复核线索，不能单独触发不覆盖建议或真实拒赔。

## 8. 仿真指标

| 指标 | 口径 | 成熟度 |
|---|---|---|
| 受影响案件量 | 新旧保障建议或赔付金额发生变化 | \`OBSERVED_REPLAY\` |
| 覆盖建议增量 | 候选覆盖建议量－现行覆盖建议量 | \`OBSERVED_REPLAY\` |
| 核定赔付变化 | 候选核定赔付－现行核定赔付 | \`ESTIMATE\` |
| 人工工时变化 | 人审0.45小时/案、补件0.15小时/案 | \`ASSUMPTION\` |
| 人工成本变化 | 人工工时变化×120元/小时 | \`ASSUMPTION\` |
| 策略范围与阈值 | 上海、肠胃炎、200→500 | \`EXPERIMENT_DESIGN\` |

所有页面指标必须显示成熟度，不把历史回放或业务假设表述为真实未来预测。

后续商业测算可增加留存/转化假设、保费价值、赔付成本和盈亏平衡点，但必须与历史回放指标分层展示。

## 9. 隐私、医疗与合规

- 演示数据不得包含真实姓名、手机号、账户、芯片号或医疗记录。
- 密码、Token和完整数据源地址不得写入浏览器持久化存储；界面和图谱仅展示脱敏端点与局部编号。
- 模型API Key不得进入LocalStorage、接口响应、日志或配置文件；只允许来自环境变量或当前Node服务进程内存。
- 本地文件不上传服务器，不把文件内容写入 \`localStorage\`。
- 每个建议保存快照、规则版本、命中事实、证据位置、金额公式和关联证据路径。
- OCR、VLM或本体关系置信度不足时进入人工确认。
- 医疗诊断、除外责任和最终拒赔必须由具备权限的专业人员负责。
- 页面始终明确“模拟资料”“本地模拟识别”和“离线沙箱”。

## 10. 验收标准

- 宠物险默认进入材料审核，全部Hash路由支持直接访问、刷新、前进和后退。
- 宠物险界面和产品文档不再出现旧图谱模型、路径规划或技术权限术语。
- 诊疗图片在材料审核页内形成ImageReviewResult；本体图谱在有效人工审核结果形成前锁定。
- 完成材料审核后的第二步按钮显示“匹配跨平台身份并构建本体图谱”。
- 旧 \`#/pet/review\` 地址自动跳转至 \`#/pet/intake?evidence=image\`。
- FactSnapshot在本体事实与关系人工确认前不可冻结；未冻结快照时策略、规则检查和仿真不可执行。
- 修改材料、图像审核结果或本体关系后，已有本体确认状态、FactSnapshot、规则检查和仿真结果必须失效。
- 未点击“模型解析”前不显示解析结果；点击后通过本地Node代理调用OpenAI兼容接口，并映射为运营中文表单。
- 未配置模型时进入 \`#/settings\`；设置页可测试、保存和清除进程内配置，并可返回策略实验。
- 自然语言输入能够解析范围、多个条件、统一AND/OR、命中/未命中动作和空值策略，并允许人工修正。
- “超过500元”映射为 \`>\`，“不少于500元”映射为 \`>=\`；“以上、以下、左右、偏高”等表达必须人工确认或修正。
- 范围、空值、ELSE动作、字段值、动作或比较符缺失时禁止确认；混合AND/OR、括号嵌套、未知字段和最终拒赔动作必须阻断。
- 确认后的CandidateRule、DSL、规则检查、仿真与案例条件轨迹必须完全一致。
- 401、429、超时、断网、错误JSON、未知字段、嵌套逻辑和最终拒赔动作均返回通俗错误，不污染已保存候选规则。
- 材料完整度和图片审核事实只能进入人工复核、请求补件或沿用现行路由，不会形成自动审核、不覆盖建议或最终拒赔。
- 默认200→500规则只有上海肠胃炎案件受阈值变化影响；用户确认的通用候选规则按其适用范围执行。
- 200、200.01、500、500.01严格符合大于号语义。
- 相同种子、快照、版本和策略重复仿真得到一致结果。
- KPI、分布图、环图、迁移数量和案例明细完全一致。
- 每项指标显示正确成熟度。
- 诊疗图片页面不出现OCR字段；人工审核结果可保存、修订和追踪源材料，但不会直接拒赔。
- 结论和问题标签必选；PASS意见选填，FAIL或NEEDS_REVIEW意见必填。
- 图谱正确展示“团子 · ****668”及五个平台，且编号、账户、联系方式和芯片号均已脱敏。
- 图谱实体均以圆形气泡展示，初始中心为“团子 · ****668”，周围准确显示五个平台。
- 点击平台、类别和记录气泡后，目标气泡进入中心并展示一度上下游；记录中心显示四个只读属性气泡。
- 路径导航、返回上级、回到团子和键盘聚焦均可用；右侧面板与当前中心实体一致。
- 聚焦切换在2秒内完成，中心节点精确落在SVG中心，页面与滚动位置不刷新；无卡顿、闪屏、重复节点或点击穿透，非中心气泡保持虚化。
- 芯片或平台编号精确匹配、保单与医院档案匹配、组合匹配及低置信度待核验状态显示正确。
- 关联证据路径完整展示宠物主实体、平台局部实体、身份匹配依据、类别记录、来源证据和确认状态。
- 本体图谱标题卡显示“数据接入”，数据库和其他平台表单按类型切换，必填、端口、URL、重复源和数量上限校验正确。
- 未通过连接测试时不能确认接入；接入后根节点立即新增待核验平台气泡，刷新后保持一致，断开后平台、类别与记录同步消失。
- 外接平台最多5个、总平台最多10个；新增或断开只局部更新图谱和右侧面板，不刷新页面或改变滚动位置。
- localStorage不得包含密码、Token或完整敏感地址；相同安全配置生成一致的数据类别、记录摘要和节点ID。
- 数据连接变化保留材料审核与图谱构建进度，但必须使本体确认、FactSnapshot、候选策略确认、规则检查和仿真结果失效。
- 未人工确认本体关系时不能冻结 FactSnapshot；确认后可进入数据与快照页面。
- 医院、支付和家系关系只形成事实或风险线索，不会形成自动拒赔。
- 1024px和1440px无页面级横向滚动，键盘操作可用，控制台无错误。
- 信贷页面、旧信贷Hash地址和信贷本地状态不受影响。

## 11. 面试演示脚本

1. 在材料审核页登记文本材料、核对OCR spans/bbox并完成结构化字段标准化。
2. 切换至诊疗图片，选择人工结论和必选问题标签；演示FAIL或NEEDS_REVIEW时意见必填。
3. 保存ImageReviewResult并进入本体图谱，说明人工图片结论只改变分流。
4. 在本体图谱标题卡打开“数据接入”，配置数据库或平台接口、执行本地模拟测试并确认接入；观察根节点新增待核验平台气泡。
5. 从“团子 · ****668”依次聚焦新平台、数据类别和具体记录，观察气泡居中动画、路径导航和右侧证据联动，再人工确认本体关系。
6. 在数据页冻结FactSnapshot，展示固定种子、版本链和内容哈希。
7. 在策略实验输入自然语言规则，点击“模型解析”，在运营中文表单中核对并修正；展开技术详情查看边界样本、DSL和模型请求信息，再一次确认进入规则检查。
8. 在仿真总览运行baseline/challenger双跑，查看分布、建议构成、赔付和工时变化。
9. 点击迁移进入案例追踪，核对200.01、500和500.01的边界差异。
10. 回到材料审核修改诊疗图片结果，展示下游快照和仿真自动失效。
11. 切换到信贷解决方案，证明通用外壳、规则和仿真内核可复用。

## 12. 后续集成路线

- 接入真实OCR与版面分析；VLM只提供图片质量提示，正式结论仍由人工审核确认。
- 当前由FastAPI单服务提供静态页面、模型代理、业务API和审计接口；SQLite保存演示事实、版本对象和仿真结果，`DATABASE_URL`预留PostgreSQL切换能力。
- 使用 DuckDB/Polars/Parquet 扩展大规模批量历史回放；当前320笔案件由同步Python规则内核确定性执行。
- 在关系规模和查询复杂度达到阈值后引入图数据库；MVP不以Neo4j为前置条件。
- 将当前确定性模拟连接器升级为后端连接网关、密钥管理、连接池、最小权限账号及数据库/REST/SFTP/Webhook真实适配器，再接入保单、医院、发票核验、支付和人工审核系统。
- 增加策略审批、影子运行、灰度发布、线上监控和决策复盘。


## 13. v20 FastAPI后端闭环

### 13.1 服务与持久化

- FastAPI统一监听`127.0.0.1:4173`，同时托管原生HTML/CSS/JavaScript与`/api/*`。
- 同步SQLAlchemy管理案件、证据、审核、连接、本体、快照、策略、仿真和审计对象；Alembic维护数据库版本。
- 默认数据库为`data/strategy_sandbox.db`，上传文件保存在`data/uploads/`，均不进入代码仓库。
- 固定种子`20260814`首次生成320笔案件，并保留199、200、200.01、499.99、500、500.01元边界夹具。

### 13.2 一致性与失效

- 所有业务修改提交`experimentRevision`；旧页面修改返回`409 STALE_STATE`，避免多标签页覆盖。
- FactSnapshot、已确认策略和SimulationRun均按内容哈希生成不可变版本。
- 材料或图片变化会使本体确认及全部下游结果失效；连接或关系变化使快照、策略和仿真失效；策略变化只使检查和仿真失效。
- 失效对象保留审计历史，不物理删除；活动指针只指向当前有效版本。

### 13.3 安全边界

- 上传白名单为PNG、JPEG、WebP和PDF，单文件不超过10MB，服务端计算SHA-256并使用内部生成路径。
- 数据连接凭据只在测试请求和5分钟一次性`testToken`中短暂存在，数据库仅保存脱敏地址、摘要、版本和指纹。
- 模型API Key只来自环境变量或FastAPI进程内存，不进入数据库、浏览器存储、日志或候选规则。
- 模型只负责结构化；字段白名单、动作权限、金额计算、规则检查和仿真执行均由确定性Python内核完成。

### 13.4 启动与验证

1. `python -m pip install -r requirements.txt`
2. `npm start`
3. 打开`http://127.0.0.1:4173/`
4. `npm test`运行Python闭环测试与前端静态回归。
